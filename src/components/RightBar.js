import React from 'react';
import PropTypes from 'prop-types';

const RightBar = props => {
    return (
        <div>
            
        </div>
    );
};

RightBar.propTypes = {
    
};

export default RightBar;