import React from 'react';
import PropTypes from 'prop-types';

const UserInfo = props => {
    return (
        <div>
            
        </div>
    );
};

UserInfo.propTypes = {
    
};

export default UserInfo;